A few things to tackle next:

1. The rides list still looks plain — turn them into cards instead of bullet points
2. Some of that test data is clearly fake (123456789km) — clean the database again
3. The navbar just says "Home" which is redundant — change it to your app name like "CycleLog" or whatever you want to call it